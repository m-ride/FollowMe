# Neon en producción, docker compose en local
DATABASE_URL=postgres://postgres:finanzas@localhost:5432/finanzas?sslmode=disable
# Token compartido: sin él la API queda abierta. Obligatorio en Render.
APP_TOKEN=cambia-esto
# Origen del frontend en Vercel; "*" en local
CORS_ORIGIN=*
